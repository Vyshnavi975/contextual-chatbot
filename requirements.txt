# Core dependencies: none required -- the chatbot runs fully offline in
# "demo mode" using only the Python standard library.
#
# Optional dependencies below unlock real LLM-backed responses and/or the
# web UI. Install whichever you need:
#
#   pip install -r requirements.txt
#
# or install a subset, e.g.:
#   pip install openai
#   pip install flask

openai>=1.40.0
flask>=3.0.0

# Testing
pytest>=7.4.0
